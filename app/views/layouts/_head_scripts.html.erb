<!--
  MWD-628 Font Flicker - We moved typkit include to the header to attempt to reduce font flash's occurance. Also meets typekit instructions for implementation.
-->
  <script type="text/javascript" src="//use.typekit.net/lkh8dve.js" onload="try{Typekit.load();}catch(e){}" async="async"></script>
<!--  END MWD-628 -->

<!-- Google analytics setup - run this part before any other _gaq.push'es in any other pages, (like paycom) -->
<script type="text/javascript" async="async">
  var _gaq = _gaq || [];
  _gaq.push(['_setAccount', 'UA-33561868-1']);
  _gaq.push(['_setDomainName', 'lootcrate.com']);
  _gaq.push(['_setAllowLinker', true]);
  _gaq.push(['_trackPageview']);
</script>

<!-- Facebook Custom Audience pixel (also contains a noscript pixel below) -->
<script>
  (function() {
    var _fbq = window._fbq || (window._fbq = []);
    if (!_fbq.loaded) {
      var fbds = document.createElement('script');
      fbds.async = true;
      fbds.src = '//connect.facebook.net/en_US/fbds.js';
      var s = document.getElementsByTagName('script')[0];
      s.parentNode.insertBefore(fbds, s);
      _fbq.loaded = true;
    }
    _fbq.push(['addPixelId', "277942772366596"]);
  })();
  window._fbq = window._fbq || [];
  window._fbq.push(["track", "PixelInitialized", {}]);
</script>

<% if show_countdown_timer? %>
  <script type="text/javascript" async="async">var countdowntimer_soldout = <%= GlobalConstants::SOLDOUT ? true : false %>;</script>
<% end %>

 <!-- JavaScript (jquery) needs to come before this because optimizely has been set to not include jquery -->
<% if "production" == Rails.env.downcase %>
  <script src="//cdn.optimizely.com/js/142694596.js"></script>
<% elsif "staging" == Rails.env.downcase %>
  <script src="//cdn.optimizely.com/js/2341400033.js"></script>
<% end %>

<%= render 'shared/friendbuy_smartkey' %>
